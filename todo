BufWriteCmd
  split by 70
try file creation

format
  Subsequent lines starting with a semicolon would be ignored by software
  Anything other than a valid character would be ignored (including spaces, tabulators, asterisks, etc...)
  end the sequence with an "*" (asterisk) character
stdin/stdout
  ${TMPDIR-/tmp}
sync 3 lists
  append line
  prepend line
  delete line
  lines in insert mode
cut comments up to space
